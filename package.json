{
  "name": "guild",
  "private": false,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Local-first talent bench for AI bots. @mention the one you want. Not another omniscient chat.",
  "type": "module",
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "pnpm --filter @guild/daemon dev",
    "dev:desktop": "pnpm --filter @guild/desktop dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @guild/daemon test"
  }
}
